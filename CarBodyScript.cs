using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class CarBodyScript : MonoBehaviour
{
    public bool hit;
    public Rigidbody rb;
    public float respawnCountdown = 2.5f;

    // Start is called before the first frame update
    void Start()
    {
        
    }    
}
